<keypress-events on-save="safeSubmit(false, false, false)" on-next="safeSubmit(true, false, true)" on-add="newReviewOfSystems()" permission-key="Clinical-ReviewOfSystems-ReviewofSystems" permission="{{resolvePermission(reviewOfSystem.RoSID)}}"></keypress-events>
<div class="panel panel-light padding-xsmall">
    <div class="panel-body">

        <form role="form" name="ctrl.reviewOfSystemsForm" check-form data-on-save="save(isNext, isMandatory, hasErrors, keepForm)" novalidate bs-modifiable="true">
            {{ setform(ctrl.reviewOfSystemsForm.modified, ctrl.reviewOfSystemsForm.name) }}

            <div class="row">
                <div class="col-md-12">
                    <div ui-view>
                    </div>
                    <div security permission-key="Clinical-ReviewOfSystems-ReviewofSystems" permission="{{resolvePermission(reviewOfSystem.RoSID)}}" permission-mode="readonly">
                        @Html.Partial("../Shared/_TakenDetails")
                    </div>
                </div>
            </div>
            <div class="row padding-top-small">
                <div class="col-lg-6 col-md-6">
                    <p class="uppercase subtext"><sup>*</sup> Indicates a required field</p>
                </div>
                <div class="col-lg-6 col-md-6 text-right">
                    <button type="button" class="btn btn-default btn-lg" data-ng-click="safeSubmit(false, false, false)" id="btnSave" name="btnSave" security permission-key="Clinical-ReviewOfSystems-ReviewofSystems" permission="{{resolvePermission(reviewOfSystem.RoSID)}}">SAVE</button>
                    <button type="button" class="btn btn-default btn-lg" data-ng-click="safeSubmit(true, false, true)" id="btnNext" name="btnNext" security permission-key="Clinical-ReviewOfSystems-ReviewofSystems" permission="{{resolvePermission(reviewOfSystem.RoSID)}}">NEXT</button>
                </div>
            </div>
            <div class="add_me">
                <plus-button title="New Review Of Systems" save="safeSubmit(false, false)" permission-key="Clinical-ReviewOfSystems-ReviewofSystems" permission="create" reset="newReviewOfSystems()"></plus-button>
            </div>
            <div class="row padding-top-small padding-bottom-small">
                <div class="col-md-12">
                    <table id="reviewOfSystemsTable" data-toggle="table"
                           data-show-columns="true"
                           data-search="true"
                           data-minimum-count-columns="2"
                           data-filter-control="false"
                           data-mobile-responsive="true"
                           data-filter-bar
                           data-advanced-search="false"
                           data-show-multi-sort="true"
                           data-classes="table table-stripe-borders"
                           bootstrap-table="tableoptions" grid-arrow-selector>
                        <thead>
                            <tr>
                                <th data-field="ReviewdByName"
                                    data-sortable="true">TakenBy</th>
                                <th data-field="DateEntered"
                                    data-sortable="true">TakenDate</th>
                                <th></th>
                            </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </form>

    </div>
</div>
